# Public 静态资源目录

此目录用于存放 Next.js 直接公开访问的静态资源。

## 站点图标

当前站点统一使用：

- `site-icon.png`：浏览器标签页 favicon 与 Apple Touch Icon 共用，固定为 `180x180` PNG

如需新增全站静态资源，放在本目录后可通过根路径访问，例如 `public/example.png` 对应 `/example.png`。
