# Public 静态资源目录

此目录用于存放静态资源文件，如 favicon、图片等。

## Favicon 配置

请将你的 favicon 文件放在此目录下：

- `favicon.ico` - 传统 ICO 格式（推荐 16x16 或 32x32 像素）
- `favicon.png` - PNG 格式（推荐 32x32 或 64x64 像素）
- `favicon.svg` - SVG 格式（矢量图，自适应大小）

文件放置后，VitePress 会自动从此目录提供静态文件访问。

