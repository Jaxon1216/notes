# 二分查找

视频精讲：[二分查找](https://www.bilibili.com/video/BV1AP41137w7/)

| 题目                                                                                                                                    | 代码                                                                                                                                                 | 备注       |
| --------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| [34. 在排序数组中查找元素的第一个和最后一个位置](https://leetcode.cn/problems/find-first-and-last-position-of-element-in-sorted-array/) | [代码](https://leetcode.cn/problems/find-first-and-last-position-of-element-in-sorted-array/solution/er-fen-cha-zhao-zong-shi-xie-bu-dui-yi-g-t9l9/) | 三种写法   |
| [2529. 正整数和负整数的最大计数](https://leetcode.cn/problems/maximum-count-of-positive-integer-and-negative-integer/)                  | [代码](https://leetcode.cn/problems/maximum-count-of-positive-integer-and-negative-integer/solution/mo-ni-by-endlesscheng-8e43/)                     | \*课后作业 |
| [2300. 咒语和药水的成功对数](https://leetcode.cn/problems/successful-pairs-of-spells-and-potions/)                                      | [代码](https://leetcode.cn/problems/successful-pairs-of-spells-and-potions/solution/by-endlesscheng-1kbp/)                                           | \*课后作业 |
| [1385. 两个数组间的距离值](https://leetcode.cn/problems/find-the-distance-value-between-two-arrays/)                                    | [代码](https://leetcode.cn/problems/successful-pairs-of-spells-and-potions/solutions/1595712/by-endlesscheng-1kbp/)                                  | \*课后作业 |
| [2080. 区间内查询数字的频率](https://leetcode.cn/problems/range-frequency-queries/)                                                     | [代码](https://leetcode.cn/problems/range-frequency-queries/solution/tong-ji-wei-zhi-er-fen-wei-zhi-by-endles-8l9u/)                                 | \*课后作业 |
| [2563. 统计公平数对的数目](https://leetcode.cn/problems/count-the-number-of-fair-pairs/)                                                | [代码](https://leetcode.cn/problems/count-the-number-of-fair-pairs/solution/er-fen-cha-zhao-de-ling-huo-yun-yong-by-wplbj/)                          | \*课后作业 |
| [875. 爱吃香蕉的珂珂](https://leetcode.cn/problems/koko-eating-bananas/)                                                                | [代码](https://leetcode.cn/problems/koko-eating-bananas/solution/er-fen-da-an-fu-ti-dan-pythonjavacgojsru-eb18/)                                     | \*课后作业 |
| [2187. 完成旅途的最少时间](https://leetcode.cn/problems/minimum-time-to-complete-trips/)                                                | [代码](https://leetcode.cn/problems/minimum-time-to-complete-trips/solution/er-fen-da-an-python-yi-xing-gao-ding-by-xwvs8/)                          | \*课后作业 |
| [275. H 指数 II](https://leetcode.cn/problems/h-index-ii/)                                                                              | [代码](https://leetcode.cn/problems/h-index-ii/solution/tu-jie-yi-tu-zhang-wo-er-fen-da-an-si-ch-d15k/)                                              | \*课后作业 |
| [2861. 最大合金数](https://leetcode.cn/problems/maximum-number-of-alloys/)                                                              | [代码](https://leetcode.cn/problems/maximum-number-of-alloys/solution/er-fen-da-an-fu-ti-dan-by-endlesscheng-3jdr/)                                  | \*课后作业 |
| [2439. 最小化数组中的最大值](https://leetcode.cn/problems/minimize-maximum-of-array/)                                                   | [代码](https://leetcode.cn/problems/minimize-maximum-of-array/solution/liang-chong-zuo-fa-er-fen-da-an-fen-lei-qhee6/)                               | \*课后作业 |
| [2517. 礼盒的最大甜蜜度](https://leetcode.cn/problems/maximum-tastiness-of-candy-basket/)                                               | [代码](https://leetcode.cn/problems/maximum-tastiness-of-candy-basket/solution/er-fen-da-an-by-endlesscheng-r418/)                                   | \*课后作业 |

## 例题

- 推荐开区间的二分查找算法
  - 由于索引从0开始，偶数长度的时候视觉上的mid指着的元素通常会偏左
  - 循环不变量是我们始终约定的一套规则，遵循这个规则来，退出循环，推理答案
  - 记住这个例题
- 为什么循环条件是 `l + 1 < r`？
  在 (l, r) 这个区间内，当 `l + 1 === r` 时，区间内没有元素了，停止循环。
  所以循环继续的条件是 `l + 1 < r`，等价于 r - l > 1。
- 为什么第一个 if 是 `target <= nums[mid]`？
  因为我们要找的是第一个大于等于 target 的数的下标。
    - 如果 `nums[mid] >= target`，说明第一个大于等于 target 的数可能在 mid 或 mid 左边（包括 mid 本身），所以 r 应该收缩到 mid。
    - 如果 `nums[mid] < target`，说明 mid 以及它左边的元素都小于 target，第一个大于等于 target 的在右边，所以 l 收缩到 mid。

```js
/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var lowerbound = function (nums, target) {
  let l = -1,
    r = nums.length;
  //要移动指针缩小范围，就要判断target跟中间值的大小
  //由于初始化两个开区间，那么循环条件就是r - l > 1？？不太懂对不对
  while (l + 1 < r) {
    const mid = Math.floor((l + r) / 2);
    //目标值在中间值左边，收缩右边指针，但是可能有连续的目标值，所以要有等于号
    if (nums[mid] >= target) r = mid;
    //相反
    else l = mid;
  }
  //循环结束之后，l跟r挨着，第一个target是r
  return r;
};
var searchRange = function (nums, target) {
  const start = lowerbound(nums, target);

  const end = lowerbound(nums, target + 0.5) - 1;
  //怎么，判断有没有取到正确答案？记住
  if (start === nums.length || nums[start] !== target) {
    return [-1, -1]; // nums 中没有 target
  }

  return [start, end];
};
```

## 作业
### 正整数和负整数的最大计数
略
### 咒语和药水的成功对数
- js的数组push方法
- 深入理解二分查找，排序特征
```js
/**
 * @param {number[]} spells
 * @param {number[]} potions
 * @param {number} success
 * @return {number[]}
 */

var lowerbound = function (nums, target) {
    //开区间比较形象
    let l = -1, r = nums.length;
    //循环不变量：区间大于1，先找大于等于的
    while (l + 1 < r) {
        //注意，这里不必这样写，是cpp和java可能溢出，这里别忘记去除小数，下标必须是整数
        let mid = Math.floor(l + (r - l) / 2);

        if (nums[mid] >= target) r = mid;
        else l = mid;
    }
    return r;
}
var successfulPairs = function (spells, potions, success) {
    let m = spells.length, n = potions.length;
    let tmp,index, ans = [];
    //有花括号就要有return！！！
    potions.sort((a,b)=>a-b);
    // 要想应用二分查找，要抓住有序的特征，所以spells[m] * potions[n] >= 7;
    //变成了 potions[n] >= floor(7/ spells[m]),为什么取ceil，因为假如得到3.4,用3就多了错误答案，4刚刚好
    for(let i = 0; i < m; i++){
        tmp = Math.ceil(success/spells[i]);

        index = n - lowerbound(potions,tmp);
        // console.log(i,index,tmp)
        // ans = [...ans,index];
        ans.push(index);
    }
    return ans;
};
```
### 两个数组间的距离值 -技巧
- 绝对值的情况
- 分步判断，注意二分的返回值是什么
```js
var findTheDistanceValue = function (arr1, arr2, d) {
    let ans = 0;
    arr2.sort((a, b) => a - b);
    
    for (let x of arr1) {
        // 找第一个 >= x-d 的元素
        let idx = lowerbound(arr2, x - d);
        
        // 检查这个元素是否在 [x-d, x+d] 范围内
        // 如果 idx 在数组范围内，并且 arr2[idx] <= x+d，说明区间内有元素
        if (idx < arr2.length && arr2[idx] <= x + d) {
            // 存在元素在区间内，x不符合条件
            continue;
        } else {
            // 区间内没有元素，x符合条件
            ans++;
        }
    }
    
    return ans;
```

# 附录

### 这些区别的本质是什么？

区间定义决定了边界值的含义：在闭区间中，left 和 right 都是当前可能包含答案的范围；在左闭右开中，right 是第一个不满足条件的位置（或超出范围）；在开区间中，left 是最后一个 < target 的位置，right 是第一个 >= target 的位置。

循环条件随之变化：必须保证区间内至少有一个待检查的元素。

更新规则必须维持循环不变量：这是最核心的。只要你维护好不变量，最终答案就会落在预期的边界上。

mid 落在 target 索引的位置是否受影响？
不，mid 的计算公式 Math.floor((left+right)/2) 在所有实现中都是一样的，只是 left 和 right 的值不同，导致 mid 的取值序列可能不同。但最终搜索到的目标位置是相同的，因为算法逻辑正确。mid 只是一个探针，帮助我们缩小范围，具体落在哪个索引取决于当前区间和数组分布，但最终我们会收敛到正确答案。

### 4. 为什么开区间写法可能更直观？

开区间写法的循环不变量非常清晰：

左边界的左边所有元素 < target（且左边界本身 < target）。

右边界的右边所有元素 >= target（且右边界本身 >= target）。

每次比较 mid 后，根据结果将 mid 赋给左或右边界，区间始终保持开区间，且不变量自动维持。

循环结束时，左右边界相邻，此时右边界就是第一个 >= target 的位置。

这种写法不需要在更新时进行 ±1 操作，不容易出现边界错误，因此许多算法竞赛选手偏爱这种风格。

## 为什么floor
这个问题问得太好了！这确实是二分查找的一个关键点。

## 为什么用 floor 而不是 ceil？

### 核心原因：保持区间不变量的正确性

让我们用具体例子说明：

假设数组 `[1, 3, 5, 7, 9]`，`l = -1`, `r = 5`（数组长度）

### 如果用 floor：
```javascript
mid = Math.floor((l + r) / 2) = Math.floor(4 / 2) = 2
// 得到中间偏左的位置，没问题
```

### 如果用 ceil：
```javascript
mid = Math.ceil((l + r) / 2) = Math.ceil(4 / 2) = 2  // 长度偶数时一样

// 但看另一个情况：l = 2, r = 3
mid_floor = Math.floor(5 / 2) = 2  // 指向 l
mid_ceil = Math.ceil(5 / 2) = 3    // 指向 r
```

## 关键问题：当 l 和 r 相邻时

当 `l + 1 = r` 时：
- `(l + r) / 2 = (l + l + 1) / 2 = l + 0.5`
- `floor` 得到 `l`
- `ceil` 得到 `r`

### 如果用 ceil：
```javascript
while (l + 1 < r) {
    mid = Math.ceil((l + r) / 2);  // 当 l+1=r 时，mid = r
    
    if (nums[mid] >= target) {
        r = mid;  // r 不变（还是 r）
    } else {
        l = mid;  // l 变成 r
    }
    // 区间没缩小！可能死循环
}
```

### 用 floor 就安全：
```javascript
while (l + 1 < r) {
    mid = Math.floor((l + r) / 2);  // 当 l+1=r 时，mid = l
    
    if (nums[mid] >= target) {
        r = mid;  // r 变成 l，区间缩小
    } else {
        l = mid;  // l 变成 l（不变？）
    }
    // 检查：如果 nums[l] < target，l 不变，但 r 会变
    // 无论如何，区间一定会缩小
}
```

## 另一个角度：二分查找的本质

我们是在**缩小搜索区间**：
- 用 floor 时，mid 总是偏向左边（较小的一边）
- 这样能保证每次循环都能缩小搜索范围
- 如果 mid 取 ceil，在某些边界情况下可能陷入死循环

## 总结
用 floor 是为了保证：
1. **mid 始终在区间内**（不会等于 r）
2. **区间一定能缩小**（避免死循环）
3. **算法正确性**（左右指针最终会相遇）

这是一个经过实践检验的稳定写法！

**完全正确！你理解得非常到位！** 👍

## 视觉化理解

数组长度 5（奇数）：`[10, 20, 30, 40, 50]`
下标：`0   1   2   3   4`

```
中间值是下标 2 (30)
用 floor: (0+4)/2 = 2 ✓ 正好是中间
用 ceil:  (0+4)/2 = 2 ✓ 也一样
```

数组长度 6（偶数）：`[10, 20, 30, 40, 50, 60]`
下标：`0   1   2   3   4   5`

```
中间有两个值：下标 2(30) 和 3(40)
用 floor: (0+5)/2 = 2.5 → floor = 2 ✓ 取左边的中间值
用 ceil:  (0+5)/2 = 2.5 → ceil  = 3 ✗ 取右边的中间值
```

## 为什么取左边更自然？

因为**下标从 0 开始**，二分查找的视觉中心自然偏左：
```
[10, 20, 30, 40, 50, 60]
  ↑        ↑        ↑
  l       mid       r
 0        2        5
```
左边有 2 个元素，右边有 3 个元素，左边略少，符合 "floor 取整" 的直觉。

## 数学本质
```javascript
(l + r) / 2 在偶数长度时是 .5
floor 取左边整数
ceil  取右边整数
```

二分查找的标准实现都选 floor（左边），这样：
- 代码简单
- 避免边界死循环
- 符合编程直觉

你的理解完全正确！

## js数组方法
哈哈，这个问题问得好！是的，JavaScript 数组有 `push` 方法，而且是非常常用的方法！

## push 方法示例

```javascript
let arr = [1, 2, 3];
arr.push(4);        // arr 变成 [1, 2, 3, 4]
arr.push(5, 6);     // 可以一次 push 多个： [1, 2, 3, 4, 5, 6]

console.log(arr);   // [1, 2, 3, 4, 5, 6]
```

## 你的两种写法对比

### 你现在的写法（用展开运算符）：
```javascript
ans = [...ans, count];  // 每次创建新数组
// 比如 ans = [1, 2], count = 3
// 过程：创建新数组 [1, 2, 3]，然后赋值给 ans
```

### 用 push 的写法：
```javascript
ans.push(count);  // 直接在原数组上添加
// 比如 ans = [1, 2], count = 3
// ans 直接变成 [1, 2, 3]，没有创建新数组
```

## 区别和选择

| 方法 | 是否创建新数组 | 性能 | 返回值 |
|------|---------------|------|--------|
| `push` | 否（修改原数组） | 高 | 新数组长度 |
| 展开运算符 `[...ans, count]` | 是（创建新数组） | 较低 | 新数组 |

对于你的循环场景（m 可能很大），用 `push` 性能更好，因为它不会每次都创建新数组。

## 其他常用数组方法
```javascript
let arr = [1, 2, 3];

arr.push(4);           // 末尾添加 → [1, 2, 3, 4]
arr.pop();             // 末尾删除 → [1, 2, 3]
arr.unshift(0);        // 开头添加 → [0, 1, 2, 3]
arr.shift();           // 开头删除 → [1, 2, 3]
```

所以你可以放心用 `push`，它是 JavaScript 数组的基础方法！